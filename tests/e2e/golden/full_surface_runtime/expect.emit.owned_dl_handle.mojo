def _bindgen_dl(
_bindgen_dl().call[
struct surf_point
struct surf_pair
struct surf_flags
struct surf_matrix
struct surf_packed_header
UnsafeUnion
def surf_add(
def surf_affine(
def surf_apply_mode(
def surf_is_nonzero(
def surf_fill_series(
def surf_sum_array(
def surf_count_nonzero(
def surf_memory_copy(
def surf_get_message(
def surf_handle_new(
def surf_install_callback(
SURF_MAGIC
